using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class InputController : MonoBehaviour
{

    



    // Start is called before the first frame update
    void Start()
    {
     

    }

    // Update is called once per frame
    void Update()
    {
          // if(Input.GetKey(AddScale))
          // {
          //   Cube1.transform.localScale += new Vector3(Time.deltaTime, Time.deltaTime, Time.deltaTime); 
          // }

          // if(Input.GetKey(RemoveScale))
          // {
          //   Cube1.transform.localScale -= new Vector3(Time.deltaTime, Time.deltaTime, Time.deltaTime); 
          // }

          // if(Input.GetKey(KeyCode.R))
          // {

          //   Cube1.transform.localEulerAngles += new Vector3(0, Time.deltaTime * RotationSpeed, 0); 
          //  // Cube1.transform.rotation = Quaternion.Euler(0, Time.deltaTime * RotationSpeed, 0); 
          // }


          // if(Input.GetKeyDown(KeyCode.UpArrow))
          // {
          //   RotationSpeed++;
          // }
          //   if(Input.GetKeyDown(KeyCode.DownArrow))
          // {
          //   RotationSpeed--;
          // }

        
        




    }
}
